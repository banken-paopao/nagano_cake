  <table class="table">
    <thead>
      <tr>
        <th>ジャンル検索</th>
      </tr>
    </thead>
    <tbody>
      <% genres.each do |genre| %>
        <tr>
          <td>
            <%= link_to genre.name, item_search_items_path(genre_id: genre.id, model: "genre"), class: "rounded-pill genre-tag" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>